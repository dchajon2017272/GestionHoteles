import { SearchReservacionPipe } from './search-reservacion.pipe';

describe('SearchReservacionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservacionPipe();
    expect(pipe).toBeTruthy();
  });
});
